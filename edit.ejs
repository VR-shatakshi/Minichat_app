<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit screen</title>
</head>
<body>
     
    <h3> Edit your chat :<%= chats._id %></h3>

    <p> This chat was sent from <b><%= chats.from %></b> to  <b><%= chats.to %></b></p>

    <form method ="POST" action = "/chats/<%=chats._id %>?_method=PUT">
        <textarea name ="msg" rows="6" cols="5" > <%=chats.msg %></textarea>
        <br>
       <button>Edit </button>
    </form>
</body>
</html>